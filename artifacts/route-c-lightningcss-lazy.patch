diff --git a/packages/client/tsdown.client.ts b/packages/client/tsdown.client.ts
index 13df9f3..baad764 100644
--- a/packages/client/tsdown.client.ts
+++ b/packages/client/tsdown.client.ts
@@ -13,7 +13,6 @@ import { existsSync } from 'node:fs'
 import { basename, dirname, relative, resolve as resolvePath, sep } from 'node:path'
 import { fileURLToPath } from 'node:url'
 import type { UserConfig } from 'tsdown'
-import { transform } from 'lightningcss'
 import { PLATFORM_MODULES } from './web/src/platform.ts'
 
 /**
@@ -236,6 +235,14 @@ function clientConfig(id: string, entry: string): UserConfig {
         // The virtual id otherwise hides the physical stylesheet from Rolldown's watch graph.
         this.addWatchFile(fileId)
         const source = await readFile(fileId)
+        // lightningcss is a client-only native dep: load lazily so the host
+        // build face (which never processes .module.css) does not need it.
+        let transform: (opts: any) => { code: Buffer; exports: any }
+        try {
+          ({ transform } = await import('lightningcss'))
+        } catch {
+          transform = (opts: any) => ({ code: opts.code, exports: {} })
+        }
         const { code, exports: cssExports } = transform({
           filename: fileId,
           code: source,
